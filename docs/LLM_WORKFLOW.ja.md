# LLM 作業手順

1. `AGENTS.md` と `APP_SPEC.md` を読む。
2. `src/index.template.html` の現状を確認する。
3. ローカルファースト・単一HTML・実行時外部通信なしを維持する。
4. ソース、設定、ドキュメントを同じ変更で更新する。
5. Windowsで `scripts/check-repository.ps1` を実行する。
6. 2種類の生成HTMLとmanifestを確認する。
