# Third-party notices

Version 1.0 has no runtime third-party JavaScript, CSS, font, WASM, or other embedded package dependencies.

The application uses browser-native APIs and project-authored code only. If a dependency is added later, pin it in `dependencies.json` and record its license and homepage here.
